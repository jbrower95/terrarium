{
  "wallet": "{{walletAddress}}",
  "models": {
    "owner": "{{ownerModel}}",
    "high": "{{highModel}}",
    "medium": "{{mediumModel}}",
    "low": "{{lowModel}}"
  },
  "owner_cron": "{{ownerCron}}",
  "max_concurrent_employees": {{maxConcurrentEmployees}},
  "budget": {
    "downgrade_threshold_usd": {{downgradeThreshold}},
    "pause_threshold_usd": {{pauseThreshold}}
  }
}
